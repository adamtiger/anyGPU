from setuptools import setup, find_packages

setup(name='dnninspect',
      version='0.1.0',
      description='Inspecting complex dnn networks like LLMs',
      url='https://github.com/adamtiger/anygpu',
      author='Adam Budai',
      author_email='',
      license='MIT',
      packages=find_packages(),
      zip_safe=False)
